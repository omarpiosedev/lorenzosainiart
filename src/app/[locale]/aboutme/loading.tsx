import { AboutSkeleton } from '@/components/ui/LoadingSkeleton';

/**
 * Next.js 16 Instant Loading UI for About Me
 * Automatically wrapped in Suspense by Next.js
 */
export default function Loading() {
  return <AboutSkeleton />;
}
