import { PortfolioSkeleton } from '@/components/ui/LoadingSkeleton';

/**
 * Next.js 16 Instant Loading UI for Portfolio
 * Automatically wrapped in Suspense by Next.js
 */
export default function Loading() {
  return <PortfolioSkeleton />;
}
